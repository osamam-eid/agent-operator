{
  "name": "@omp-agent/agent-operator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Isolated Stage 4 Agent Operator candidate: trusted policy/config resolution and deterministic workflow compilation, executing SINGLE/PARALLEL/PIPELINE graphs over native OMP child sessions (omp-task) or, in tests only, the deterministic mock adapter.",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "hash-bundle": "bun scripts/hash-bundle.ts"
  },
  "peerDependencies": {
    "@oh-my-pi/pi-coding-agent": "17.3.5"
  },
  "peerDependenciesMeta": {
    "@oh-my-pi/pi-coding-agent": {
      "optional": true
    }
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "17.3.5",
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
